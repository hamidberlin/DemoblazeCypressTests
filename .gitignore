# node_modules – externe Abhängigkeiten (werden aus package.json installiert)
node_modules/

# Cypress generiert Screenshots & Videos bei Tests – oft nicht nötig im Repo
# cypress/screenshots/
  cypress/videos/

# Temporäre Testdaten oder Fixtures (optional ignorieren, wenn dynamisch)
!cypress/fixtures/users.json

# Build-Output (z. B. für Web-Apps, falls vorhanden)
dist/
build/

# Log-Dateien
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Systemdateien (nur Mac)
.DS_Store

# VSCode Settings – nur wenn du persönliche Settings nicht teilen willst
.vscode/

# Umgebungsvariablen (falls vorhanden)
.env
.env.local
.env.*.local

# Coverage-Berichte (falls mit Code Coverage getestet wird)
coverage/

# Lokale Git-Konfigs
.gitlocal

# Cypress cache (optional)
.cypress_cache/
